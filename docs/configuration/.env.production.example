# ============================================================================
# HIKING PORTAL - PRODUCTION ENVIRONMENT TEMPLATE
# ============================================================================
# This file shows the structure for PRODUCTION environment variables
# NEVER use these values in production - they are examples only
# Set actual values in your production environment (Cloud Run, etc.)
# ============================================================================

# ============================================================================
# PRODUCTION DATABASE CONFIGURATION (Cloud SQL)
# ============================================================================
# Database connection for Google Cloud SQL
DATABASE_URL=postgresql://[USERNAME]:[PASSWORD]@[HOST]:[PORT]/[DATABASE]
# OR using Cloud SQL socket (recommended for Cloud Run)
DB_HOST=/cloudsql/[PROJECT-ID]:[REGION]:[INSTANCE-NAME]
DB_USER=[PRODUCTION_DB_USER]
DB_PASSWORD=[PRODUCTION_DB_PASSWORD]
DB_NAME=[PRODUCTION_DB_NAME]
DB_PORT=5432

# ============================================================================
# PRODUCTION BACKEND CONFIGURATION
# ============================================================================
NODE_ENV=production
PORT=8080
JWT_SECRET=[LONG_RANDOM_SECRET_MINIMUM_64_CHARACTERS]
FRONTEND_URL=https://[YOUR-DOMAIN].com

# ============================================================================
# PRODUCTION FRONTEND CONFIGURATION
# ============================================================================
REACT_APP_API_URL=https://[YOUR-API-DOMAIN].com
REACT_APP_ENV=production

# ============================================================================
# PRODUCTION SECURITY & FEATURES
# ============================================================================
DEBUG=false
LOG_LEVEL=info
ENABLE_CORS=false
SOCKET_IO_ENABLED=true
SOCKET_IO_CORS_ORIGIN=https://[YOUR-DOMAIN].com

# ============================================================================
# PRODUCTION EMAIL CONFIGURATION
# ============================================================================
SENDGRID_API_KEY=[PRODUCTION_SENDGRID_KEY]
FROM_EMAIL=noreply@[YOUR-DOMAIN].com

# ============================================================================
# PRODUCTION EXTERNAL SERVICES
# ============================================================================
# Redis (Google Cloud Memorystore)
REDIS_URL=redis://[REDIS-HOST]:[REDIS-PORT]

# ============================================================================
# PRODUCTION API KEYS
# ============================================================================
# Weather API (Production key)
WEATHER_API_KEY=[PRODUCTION_WEATHER_KEY]

# Maps API (Production key with domain restrictions)
MAPS_API_KEY=[PRODUCTION_MAPS_KEY]

# Payment Processing (Stripe live keys)
STRIPE_SECRET_KEY=sk_live_[PRODUCTION_STRIPE_SECRET]
STRIPE_PUBLISHABLE_KEY=pk_live_[PRODUCTION_STRIPE_PUBLIC]

# SMS (Twilio production credentials)
TWILIO_ACCOUNT_SID=[PRODUCTION_TWILIO_SID]
TWILIO_AUTH_TOKEN=[PRODUCTION_TWILIO_TOKEN]
TWILIO_PHONE_NUMBER=[PRODUCTION_PHONE_NUMBER]

# ============================================================================
# PRODUCTION MONITORING & LOGGING
# ============================================================================
# Application monitoring
SENTRY_DSN=[PRODUCTION_SENTRY_DSN]
NEW_RELIC_LICENSE_KEY=[NEW_RELIC_KEY]

# Google Cloud specific
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json